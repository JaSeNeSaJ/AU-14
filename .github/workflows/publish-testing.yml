name: Publish Testing

concurrency:
  group: publish-testing
  cancel-in-progress: true

on:
  workflow_dispatch:
  schedule:
    - cron: '0 10 * * *'

env:
  DOTNET_VERSION: 10.0.x
  CONFIGURATION: Release

jobs:
  package:
    name: Package ${{ matrix.name }}
    if: vars.PUBLISH_TESTING == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: client
            command: client
          - name: server-win-x64
            command: server --platform win-x64
          - name: server-linux-x64
            command: server --platform linux-x64
          - name: server-osx-x64
            command: server --platform osx-x64
          - name: server-linux-arm64
            command: server --platform linux-arm64

    steps:
      - uses: actions/checkout@v6.0.2
        with:
          persist-credentials: true
          submodules: false

      - name: Checkout submodules
        run: |
          git config --global url."https://x-access-token:${{ github.token }}@github.com/".insteadOf "https://github.com/"
          git submodule update --init --recursive --depth=1

      - uses: actions/setup-dotnet@v5.2.0
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          cache: true
          cache-dependency-path: |
            **/*.csproj
            **/*.props
            **/*.targets
            **/packages.lock.json

      - name: Restore dependencies
        run: dotnet restore

      - name: Build Packaging
        run: dotnet build Content.Packaging --configuration ${{ env.CONFIGURATION }} --no-restore /m

      - name: Package ${{ matrix.name }}
        run: dotnet run --project Content.Packaging --configuration ${{ env.CONFIGURATION }} --no-build -- ${{ matrix.command }}

      - name: Upload ${{ matrix.name }} package
        uses: actions/upload-artifact@v7.0.1
        with:
          name: release-${{ matrix.name }}
          path: release/*.zip
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  publish:
    name: Publish testing version
    if: vars.PUBLISH_TESTING == 'true'
    runs-on: ubuntu-latest
    needs: package
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          persist-credentials: true
          submodules: false

      - name: Checkout RobustToolbox
        run: |
          git config --global url."https://x-access-token:${{ github.token }}@github.com/".insteadOf "https://github.com/"
          git submodule update --init --depth=1 RobustToolbox
          cd RobustToolbox
          git fetch --depth=1

      - uses: actions/download-artifact@v8.0.1
        with:
          pattern: release-*
          path: release
          merge-multiple: true

      - name: Publish version
        run: Tools/publish_multi_request.py --fork-id cmu-testing
        env:
          PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
